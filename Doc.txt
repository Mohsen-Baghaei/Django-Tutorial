### . ### => while we want to import sth that it's in a same folder => from . import ...

pip freeze > requirements.txt


if we want to do sth simple into our database:
py manage.py shell

then we import the models
from posts.models import post

p = Post()
p.title = "My 1st post"

p.save()

Post.objects.all()
returns what posts are in database

<slug: param>

we could change the view of admin panel items by The Django admin site (search it in internet)

also we could change the theme of the admin panel 

date modification in htmls = https://docs.djangoproject.com/en/3.2/ref/templates/builtins/#date